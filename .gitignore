# Ignore application config files (if you don't want to commit them)
*.yml
*.yaml
*.properties

# Ignore Maven-specific files (except what's needed for building)
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties

# Keep Maven Wrapper (required for building)
!.mvn/wrapper/maven-wrapper.jar
!.mvn/wrapper/maven-wrapper.properties
!.mvn/wrapper/MavenWrapperDownloader.java

# Ignore Gradle-specific files (except what's needed for building)
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Ignore IDE settings and generated files
/.idea/
/.vscode/
/.classpath
/.project
/.settings/

# Ignore logs and temporary files
*.log
*.tmp
*.swp
